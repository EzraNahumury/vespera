# ReputationRegistry

Per-account score 0-1000 from on-chain counters; auto-mints reputation badges.
- Writers: registered groups (deposit/join/penalty), VotingEngine (vote participation).
- `scoreOf` / `tierOf` derive the score using the 40/20/15/10/10/5 weights.
