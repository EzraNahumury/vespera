# Contributing

1. `forge build` and `forge test` must pass before opening a PR.
2. Run `forge fmt` to format Solidity.
3. Keep changes scoped; one logical change per commit.
4. New behaviour needs a matching test in `test/`.
